class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
       //case 1
       // all even true
       // all odd true
       //case 2
       // even odd mix if one odd present
       // any num-odd gives odd 
       // so any array can be converted
       return true;
    }
};